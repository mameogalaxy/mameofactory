// modules/clouds.js — Unit 2: Volumetric clouds (STUB — implement me).
//
// Billboard/particle cloud layers drifting at altitude (the cloud sea in the image).
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'clouds',
  init(ctx) {
    // TODO(clouds): build/initialize. Billboard/particle cloud layers drifting at altitude (the cloud sea in the image).
  },
  update(dt, ctx) {
    // TODO(clouds): per-frame update (dt seconds).
  },
};
