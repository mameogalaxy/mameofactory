// modules/minimap.js — Unit 16: Minimap radar (STUB — implement me).
//
// Top-right circular canvas radar in #hud-minimap plotting ctx.world.blips + player heading cone.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'minimap',
  init(ctx) {
    // TODO(minimap): build/initialize. Top-right circular canvas radar in #hud-minimap plotting ctx.world.blips + player heading cone.
  },
  update(dt, ctx) {
    // TODO(minimap): per-frame update (dt seconds).
  },
};
