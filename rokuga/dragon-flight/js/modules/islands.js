// modules/islands.js — Unit 5: Floating islands (STUB — implement me).
//
// Several levitating rock chunks with grass tops + bottom spikes, slow bob.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'islands',
  init(ctx) {
    // TODO(islands): build/initialize. Several levitating rock chunks with grass tops + bottom spikes, slow bob.
  },
  update(dt, ctx) {
    // TODO(islands): per-frame update (dt seconds).
  },
};
