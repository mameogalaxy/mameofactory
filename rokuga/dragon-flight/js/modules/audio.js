// modules/audio.js — Unit 20: Audio (WebAudio synth) (STUB — implement me).
//
// PROVIDER: implement ctx.audio.play(name) — synth wind loop, wing-whoosh, fire roar, hit, explode. No asset files (WebAudio only).
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'audio',
  init(ctx) {
    // TODO(audio): build/initialize. PROVIDER: implement ctx.audio.play(name) — synth wind loop, wing-whoosh, fire roar, hit, explode. No asset files (WebAudio only).
  },
  update(dt, ctx) {
    // TODO(audio): per-frame update (dt seconds).
  },
};
