// modules/water.js — Unit 4: Water & waterfalls (STUB — implement me).
//
// Reflective-ish water planes for lakes/rivers + simple falling-water planes on cliffs.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'water',
  init(ctx) {
    // TODO(water): build/initialize. Reflective-ish water planes for lakes/rivers + simple falling-water planes on cliffs.
  },
  update(dt, ctx) {
    // TODO(water): per-frame update (dt seconds).
  },
};
