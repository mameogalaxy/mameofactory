// modules/hud.js — Unit 14: HUD HP/MP crest (STUB — implement me).
//
// Top-left dragon-crest HP(red)/MP(blue) bars in #hud-vitals bound to ctx.state; damage flash.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'hud',
  init(ctx) {
    // TODO(hud): build/initialize. Top-left dragon-crest HP(red)/MP(blue) bars in #hud-vitals bound to ctx.state; damage flash.
  },
  update(dt, ctx) {
    // TODO(hud): per-frame update (dt seconds).
  },
};
