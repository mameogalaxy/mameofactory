// modules/camera.js — Unit 11: Follow camera (STUB — implement me).
//
// Over-the-shoulder cam behind the dragon head, smooth lag, FOV kick on accel. Drives ctx.camera from ctx.player.group.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'camera',
  init(ctx) {
    // TODO(camera): build/initialize. Over-the-shoulder cam behind the dragon head, smooth lag, FOV kick on accel. Drives ctx.camera from ctx.player.group.
  },
  update(dt, ctx) {
    // TODO(camera): per-frame update (dt seconds).
  },
};
