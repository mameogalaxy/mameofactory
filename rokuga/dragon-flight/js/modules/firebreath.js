// modules/firebreath.js — Unit 12: Fire breath (F) (STUB — implement me).
//
// F-key flame cone particle stream from the dragon mouth; calls ctx.combat.damageAt + ctx.fx.spawn + ctx.audio.play.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'firebreath',
  init(ctx) {
    // TODO(firebreath): build/initialize. F-key flame cone particle stream from the dragon mouth; calls ctx.combat.damageAt + ctx.fx.spawn + ctx.audio.play.
  },
  update(dt, ctx) {
    // TODO(firebreath): per-frame update (dt seconds).
  },
};
