// modules/compass.js — Unit 15: Compass bar (STUB — implement me).
//
// Top-center heading strip (N/E/S/W ticks) + distance readout in #hud-compass from ctx.state.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'compass',
  init(ctx) {
    // TODO(compass): build/initialize. Top-center heading strip (N/E/S/W ticks) + distance readout in #hud-compass from ctx.state.
  },
  update(dt, ctx) {
    // TODO(compass): per-frame update (dt seconds).
  },
};
