// modules/fx.js — Unit 19: Particle FX system (STUB — implement me).
//
// PROVIDER: implement ctx.fx.spawn(type,pos,opts) for fire/hit/explosion/dust as a pooled particle system added to ctx.scene.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'fx',
  init(ctx) {
    // TODO(fx): build/initialize. PROVIDER: implement ctx.fx.spawn(type,pos,opts) for fire/hit/explosion/dust as a pooled particle system added to ctx.scene.
  },
  update(dt, ctx) {
    // TODO(fx): per-frame update (dt seconds).
  },
};
