// modules/wings.js — Unit 9: Wing-flap animation (STUB — implement me).
//
// Animate the dragon wing/tail parts each frame based on ctx.state.speed (reads ctx.player.dragonParts).
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'wings',
  init(ctx) {
    // TODO(wings): build/initialize. Animate the dragon wing/tail parts each frame based on ctx.state.speed (reads ctx.player.dragonParts).
  },
  update(dt, ctx) {
    // TODO(wings): per-frame update (dt seconds).
  },
};
