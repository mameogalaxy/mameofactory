// modules/castle.js — Unit 6: Central castle (objective) (STUB — implement me).
//
// The spired castle at map center (ctx.OBJECTIVE); registers quest objective + minimap blip.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'castle',
  init(ctx) {
    // TODO(castle): build/initialize. The spired castle at map center (ctx.OBJECTIVE); registers quest objective + minimap blip.
  },
  update(dt, ctx) {
    // TODO(castle): per-frame update (dt seconds).
  },
};
