// modules/enemies.js — Unit 18: Enemies (flying) (STUB — implement me).
//
// Wandering flying foes that ctx.combat.registerTarget, chase player, deal contact damage to ctx.state.hp, add minimap blips.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'enemies',
  init(ctx) {
    // TODO(enemies): build/initialize. Wandering flying foes that ctx.combat.registerTarget, chase player, deal contact damage to ctx.state.hp, add minimap blips.
  },
  update(dt, ctx) {
    // TODO(enemies): per-frame update (dt seconds).
  },
};
