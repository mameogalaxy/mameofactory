// modules/sky.js — Unit 1: Sky & lighting (STUB — implement me).
//
// Gradient sky dome, sun + directional/ambient light, distance fog matching the hazy blue look.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'sky',
  init(ctx) {
    // TODO(sky): build/initialize. Gradient sky dome, sun + directional/ambient light, distance fog matching the hazy blue look.
  },
  update(dt, ctx) {
    // TODO(sky): per-frame update (dt seconds).
  },
};
