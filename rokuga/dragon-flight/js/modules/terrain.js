// modules/terrain.js — Unit 3: Terrain & mountains (STUB — implement me).
//
// Procedural heightmap ground + snow-capped mountain ranges, vertex-colored.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'terrain',
  init(ctx) {
    // TODO(terrain): build/initialize. Procedural heightmap ground + snow-capped mountain ranges, vertex-colored.
  },
  update(dt, ctx) {
    // TODO(terrain): per-frame update (dt seconds).
  },
};
