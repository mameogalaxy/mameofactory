// modules/ruins.js — Unit 7: Ruins & arches (STUB — implement me).
//
// Scattered stone ruins/bridges/arches across the terrain for visual density.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'ruins',
  init(ctx) {
    // TODO(ruins): build/initialize. Scattered stone ruins/bridges/arches across the terrain for visual density.
  },
  update(dt, ctx) {
    // TODO(ruins): per-frame update (dt seconds).
  },
};
