// modules/abilities.js — Unit 13: Ability hotbar (1-4) (STUB — implement me).
//
// 4 skills (fire bolt / ice lance / shield / dash) with cooldowns + MP cost; builds #hud-hotbar DOM, binds keys 1-4.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'abilities',
  init(ctx) {
    // TODO(abilities): build/initialize. 4 skills (fire bolt / ice lance / shield / dash) with cooldowns + MP cost; builds #hud-hotbar DOM, binds keys 1-4.
  },
  update(dt, ctx) {
    // TODO(abilities): per-frame update (dt seconds).
  },
};
