// modules/quest.js — Unit 17: Quest panel (STUB — implement me).
//
// Right-side quest tracker in #hud-quest (竜の古城 / 古城の頂へ向かう); completes on reaching ctx.OBJECTIVE; emits questComplete.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'quest',
  init(ctx) {
    // TODO(quest): build/initialize. Right-side quest tracker in #hud-quest (竜の古城 / 古城の頂へ向かう); completes on reaching ctx.OBJECTIVE; emits questComplete.
  },
  update(dt, ctx) {
    // TODO(quest): per-frame update (dt seconds).
  },
};
