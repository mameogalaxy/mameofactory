// modules/dragon.js — Unit 8: Dragon model (STUB — implement me).
//
// Procedural dragon attached to ctx.player.group: body, neck, horns, dorsal spikes, 2 wings, tail. Expose parts via ctx.player.dragonParts for wings.js.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'dragon',
  init(ctx) {
    // TODO(dragon): build/initialize. Procedural dragon attached to ctx.player.group: body, neck, horns, dorsal spikes, 2 wings, tail. Expose parts via ctx.player.dragonParts for wings.js.
  },
  update(dt, ctx) {
    // TODO(dragon): per-frame update (dt seconds).
  },
};
