// modules/screens.js — Unit 21: Start / win / lose screens (STUB — implement me).
//
// Title overlay (click/Press Start -> phase=playing), win on questComplete, game-over on hp<=0, restart. Uses #hud-screens.
//
// Contract (see ../ctx.js): export default { id, init(ctx), update(dt, ctx), dispose?() }.
// Talk ONLY through ctx. Do NOT import other modules. Do NOT edit main.js / ctx.js / index.html.
// Keep the page booting green: never throw on init; guard against missing siblings.
export default {
  id: 'screens',
  init(ctx) {
    // TODO(screens): build/initialize. Title overlay (click/Press Start -> phase=playing), win on questComplete, game-over on hp<=0, restart. Uses #hud-screens.
  },
  update(dt, ctx) {
    // TODO(screens): per-frame update (dt seconds).
  },
};
